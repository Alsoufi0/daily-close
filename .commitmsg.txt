Polish landing, billing, CSV, and URLs for the live domain

- Point SITE_URL, ALLOWED_ORIGINS, and docs at the actual live alias
  daily-close-mvp.vercel.app (the cleaner daily-close.vercel.app slug is
  globally taken on Vercel).
- Landing hero now speaks broadly to store owners and surfaces the
  $29/store/month pricing with the 14-day-free line right on first paint
  so the offer is visible without a click into /billing.
- Billing page: replace the developer-only "set STRIPE_CHECKOUT_URL"
  fallback with a Start-paid-plan mailto button so an owner viewing the
  page before Stripe is wired still has a path to convert.
- Owner CSV export adds a Date column, switches Closed/Pending labels,
  and formats money to 2dp so the file is bookkeeper-ready.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
